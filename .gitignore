.github/copilot-instructions.md
.github/instructions
.github/prompts
.github/.specify

tasks/
todo.md

**.log

devCentral/

rulem
# Added by goreleaser init:
dist/

.kiro/
.vscode/
specs/
.specify/
.idea/

# compiled test binaries
*.test

# VHS demo sandbox and built binary (regenerated by demo/setup-demo-env.sh).
# Note: demo/gifs/ is intentionally NOT ignored — the README embeds those GIFs.
demo/rulem-demo
demo/sandbox/

# local credentials must never be committed
credential
creds
